---
title: "中国集群"
description: "mirobody-api.thetahealth.cn —— 中国区集群。"
---

中国集群将 100% 的数据留存于中国大陆境内，无跨境复制。

<Note>
中国区**网站尚未上线**。 —— `mirobody.cn`（首页）、`chat.mirobody.cn`（网页应用）、`platform.mirobody.cn`（开发者控制台），敬请期待。
</Note>

## 主机地址

| 界面 | 地址 |
| --- | --- |
| **API**（`base_url`） | `https://mirobody-api.thetahealth.cn/v1` |
| **开发者控制台** | `platform.mirobody.cn`（即将上线） |
| **网页应用** | `chat.mirobody.cn`（即将上线） |

控制台是你创建 `mb_live_*` 密钥、查看用量并试用 Playground 的地方。Mirobody 网页应用是旗舰产品——与本 API 背后是同一套引擎。中国区控制台上线前，请先在[全球控制台](https://platform.mirobody.ai/)创建密钥。

## 能力 (`/v1`)

<CardGroup cols={2}>
  <Card title="Agent API" icon="rocket" href="/zh/api-reference/responses">
    `POST /v1/responses` —— OpenAI Responses 兼容：会话、自带工具、MCP
  </Card>
  <Card title="Answers API" icon="comments" href="/zh/api-reference/chat">
    `POST /v1/chat/completions` —— grounded 一站式问答，流式、引用
  </Card>
  <Card title="Data" icon="database" href="/zh/api-reference/data">
    `POST/GET /v1/data` + `POST /v1/extract` —— 按 Subject 的标准化记录
  </Card>
  <Card title="Files" icon="file" href="/zh/api-reference/files">
    `POST/GET /v1/files` —— OCR / Excel 解析，WebSocket 流式上传
  </Card>
</CardGroup>

## 模型

由 Mirobody 托管——无需你自带 LLM 密钥：`mirobody-flash`（默认）/ `mirobody-expert`。见[模型](/zh/api-reference/models)。

## 存储与驻留

- 文件存储于**阿里云 OSS（杭州地域）**。
- 100% 数据留存中国大陆境内；无跨境复制。

## 合规

对齐 PIPL / 数据安全法 / 网络安全法——见[合规](/zh/api-reference/compliance)。限流见[限流](/zh/api-reference/rate-limits)。
