---
title: "模型"
description: "GET /v1/models —— 列出可用的能力档。"
---

## 端点

```http
GET /v1/models
```

列出你可以作为 `model` 传给 [Answers API](/zh/api-reference/chat) 与 [Agent API](/zh/api-reference/responses) 的能力档。形状兼容 OpenAI。

<Note>
**公开** —— 模型目录是静态能力列表（无租户数据），本端点**无需 API key**。若发送了 `Authorization` 头（OpenAI SDK 总会带上），会被直接忽略。其它所有 `/v1` 端点都需要 `Authorization: Bearer mb_live_*`。
</Note>

## 能力档

| `model` | 档位 | 适用场景 |
| --- | --- | --- |
| `mirobody-flash` | 默认 —— 快速、低成本 | 日常健康问答、高并发 |
| `mirobody-expert` | 更深入的推理 | 复杂报告解读、多步分析 |

{/* TODO(billing): 全球集群的结算币种尚未定 —— 在产品确认前保持币种中性的表述。 */}
计费按**实际 token 用量**计量 —— 控制台的[用量](/zh/api-reference/console/usage)页是消耗与费用的权威来源。自托管（SDK / 开源）是免费软件；你只需为自己的基础设施和 LLM 密钥付费。

## 示例

<CodeGroup>
```bash curl
# 无需 API key
curl https://mirobody-api.thetahealth.ai/v1/models
```

```python Python (OpenAI SDK)
from openai import OpenAI

# /v1/models 不使用 api_key，但 SDK 要求该字段有值
client = OpenAI(api_key="unused", base_url="https://mirobody-api.thetahealth.ai/v1")
for m in client.models.list().data:
    print(m.id)
```
</CodeGroup>

## 响应

一个 OpenAI 风格的 `{ "object": "list", "data": [ … ] }`。你的 `model` 取值请始终传入稳定的能力档 —— `mirobody-flash`（默认）或 `mirobody-expert`。
