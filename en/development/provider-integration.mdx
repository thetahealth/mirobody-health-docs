---
title: "Building a Vendor Client"
description: "Implement a new health-data source as a C++ vendor::Vendor: the class, the factory, and the registry entry."
---

## Overview

Adding a health-data source to Mirobody means writing a **C++ `vendor::Vendor`** — one `.cpp` file under `src/health/vendor/`, plus one line in the registry. There is no runtime plugin directory and no `BaseThetaProvider`; vendors are **compiled in** and resolved by id.

<Info>
Using existing vendors? See [Using Providers](/en/providers/using-providers). For the model behind this — the contract, honest stubs, the registry — see [Vendor System](/en/concepts/providers).
</Info>

## Prerequisites

<AccordionGroup>
  <Accordion title="Build toolchain" icon="wrench">
    A working C++11 build of the engine (`./build.sh`). See [Contributing](/en/development/contributing).
  </Accordion>
  <Accordion title="The source's public API" icon="book">
    The vendor's **public** API reference: base URL, auth scheme, the endpoint(s) for the data domains you'll support, and any webhook signature scheme. Implement only what's publicly confirmable — leave the rest an honest stub.
  </Accordion>
  <Accordion title="Credentials" icon="key">
    An API key or OAuth `client_id` / `client_secret`, read from `MIROBODY_VENDOR_<ID>_*` env vars via `VendorConfig`.
  </Accordion>
</AccordionGroup>

## The three edits

<Steps>
  <Step title="Create the vendor file">
    Pick the right bucket directory: `platform/` (B2B aggregator), `device/` (consumer brand), `phone/` (smartphone-vendor cloud API), or `ehr/`. Create `src/health/vendor/<bucket>/<id>.cpp`.
  </Step>
  <Step title="Add it to the CMake source list">
    Vendor files are listed explicitly (not globbed): add your new `.cpp` next to the sibling vendor entries in the `MIROBODY_CORE_SOURCES` list in `CMakeLists.txt` (the block right after `src/health/vendor/registry.cpp`).
  </Step>
  <Step title="Register it">
    Declare the factory and add a row in [`src/health/vendor/registry.cpp`](https://github.com/thetahealth/mirobody/blob/main/src/health/vendor/registry.cpp).
  </Step>
</Steps>

## The vendor class

A vendor is an **anonymous-namespace class** deriving from `VendorBase`, plus a free `make_<id>()` factory. It overrides only the operations it implements; everything else inherits `VendorBase`'s "not implemented" stub. Skeleton:

```cpp
// src/health/vendor/device/acme.cpp
#include "health/vendor/vendor.hpp"
#include "client/http_client.hpp"

#include <string>
#include <utility>

namespace mirobody { namespace vendor {
namespace {

class Acme : public VendorBase {
public:
    explicit Acme(VendorConfig cfg) : VendorBase(make_info(), std::move(cfg)) {}

    // Fetch a normalized DataDomain over [start_iso, end_iso]; return the vendor's JSON.
    std::string fetch(const std::string& user_id, DataDomain domain,
                      const std::string& start_iso, const std::string& end_iso) override {
        require_token();
        const std::string url = base_url() + endpoint(domain, user_id, start_iso, end_iso);
        const std::vector<std::string> headers = {
            "Authorization: Bearer " + config().api_key,
            "Accept: application/json",
        };
        client::HttpResponse res = client::HttpClient().get(url, /*timeout_ms=*/30000, headers);
        if (res.status < 200 || res.status >= 300) {
            throw VendorError(info_.id + ": fetch failed (HTTP " +
                              std::to_string(res.status) + "): " + res.body.substr(0, 300));
        }
        return res.body;
    }

    // Implement authorize_url / handle_webhook / revoke / list_providers ONLY if the
    // contract is publicly documented. Otherwise leave them as inherited stubs.

private:
    static std::string endpoint(DataDomain d, const std::string& u,
                                const std::string& s, const std::string& e) {
        switch (d) {
            case DataDomain::HeartRate: return "/v1/users/" + u + "/heart?from=" + s + "&to=" + e;
            // … the domains this source actually brokers …
            default:
                throw VendorError(std::string("acme: unsupported domain '") + to_string(d) + "'");
        }
    }

    std::string base_url() const {
        std::string b = config().base_url.empty() ? std::string("https://api.acme.example") : config().base_url;
        while (!b.empty() && b.back() == '/') b.pop_back();
        return b;
    }

    void require_token() const {
        if (config().api_key.empty()) {
            throw VendorError(info_.id + ": api_key is required — set MIROBODY_VENDOR_ACME_API_KEY");
        }
    }

    static VendorInfo make_info() {
        VendorInfo i;
        i.id                 = "acme";
        i.display_name       = "Acme Wearables";
        i.positioning        = "Consumer wearable brand with a public OAuth2 REST API";
        i.data_source_coverage = "Acme bands and watches";
        i.integration_method = "REST (OAuth2)";
        i.docs_url           = "https://developer.acme.example/";
        i.region             = Region::Global;
        i.open_source        = false;
        i.domains            = {DataDomain::Activity, DataDomain::HeartRate, DataDomain::Sleep};
        i.integrations       = {Integration::Rest};
        return i;
    }
};

}  // namespace

std::unique_ptr<Vendor> make_acme(const VendorConfig& cfg) {
    return std::unique_ptr<Vendor>(new Acme(cfg));
}

}}
```

<Tip>
Study the closest real client before writing yours: **Fitbit** (`device/fitbit.cpp`) is the model for a full OAuth 2.0 brand (fetch + authorize_url + revoke + signed webhook); **Withings** for form-encoded services; **ehr/ehr.cpp** for a FHIR R4 source; **garmin.cpp** for how to leave honest stubs.
</Tip>

## Register the factory

In `registry.cpp`, declare the factory alongside the others and add one row to the `kVendors` table (matrix order):

```cpp
// forward declaration (top of registry.cpp)
std::unique_ptr<Vendor> make_acme(const VendorConfig&);

// … inside the kVendors[] table …
{"acme", &make_acme},
```

That's the whole wiring: `open_vendor("acme", cfg)` now constructs it, and it appears in `vendor_ids()` / `all_vendor_info()`.

<Note>
Unlike MCP tools (which auto-register at compile time via a macro), vendors use this **explicit factory table** — you add the declaration and the `kVendors` row by hand.
</Note>

## The contract you're implementing

| Operation | Implement when… | Otherwise |
|-----------|-----------------|-----------|
| `info()` | **always** — populate `VendorInfo` fully | (required) |
| `fetch(user_id, domain, start, end)` | the data endpoint(s) are documented | stub |
| `authorize_url(redirect_uri, state, user_id, provider)` | consent is a plain redirect URL you can build | stub (e.g. stateful PKCE belongs elsewhere) |
| `handle_webhook(raw_headers, body)` | the signature scheme is public | stub if unsigned / undocumented |
| `revoke(user_id, provider)` | a disconnect endpoint is documented | stub |
| `list_providers(user_id)` | the source has a provider catalogue | stub / N/A for single brands |

**Rules of the road:**

- **Never invent a base URL or endpoint.** If the host is per-deployment, require `base_url` and throw if it's empty (like the `ehr` client).
- **Record why any operation is a stub** in the file header — one of: gated/undocumented, contract mismatch, no such endpoint.
- **Flag inferred field names** with a confirmed-vs-inferred comment if the vendor's reference is gated.
- **Return the vendor's JSON** from `fetch` (or FHIR JSON for FHIR-native sources). Normalization is a downstream concern.

## Config convention

Credentials come from `VendorConfig::from_env(id)`:

```
MIROBODY_VENDOR_ACME_API_KEY / _CLIENT_ID / _CLIENT_SECRET / _BASE_URL
```

`VendorConfig::configured()` is true when there's an API key or a client id+secret pair.

## Build & verify

```bash
./build.sh
./build/mirobody           # your vendor is now in the registry
```

Then verify by hand against a running server — see [Provider Testing](/en/development/provider-testing).

## Next steps

<CardGroup cols={2}>
  <Card title="OAuth Implementation" icon="key" href="/en/development/oauth-implementation">
    OAuth 2.0 / PKCE patterns in the clients
  </Card>
  <Card title="Data Mapping" icon="arrows-rotate" href="/en/development/data-mapping">
    Domains, UCUM units, and FHIR output
  </Card>
  <Card title="Provider Testing" icon="flask" href="/en/development/provider-testing">
    Verify your vendor end-to-end
  </Card>
  <Card title="Garmin / Fitbit Examples" icon="code" href="/en/examples/garmin-provider">
    Real clients against the contract
  </Card>
</CardGroup>
