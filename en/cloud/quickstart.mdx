---
title: "Quickstart"
description: "Create a key in the console and make your first /v1 call."
---

Mirobody's hosted API is **OpenAI-compatible**: point any OpenAI SDK at `https://mcp.thetahealth.cn/v1`, pass an `mb_live_*` key, and the agent answers from each end-user's real health data.

<Note>
Account management — **API keys, usage, and a live Playground** — lives in the developer console at [**cdm.thetahealth.cn**](https://cdm.thetahealth.cn/). These docs are a reference and guide; the console is where you sign in and manage everything.
</Note>

<Steps>
  <Step title="Get an API key">
    Create a key in the console — the secret is shown **once**.

    <Card title="Open the console → API Keys" icon="key" href="https://cdm.thetahealth.cn/developer/keys" horizontal>
      Sign in, create an `mb_live_*` key, and copy it.
    </Card>

    ```bash
    export MIROBODY_API_KEY="mb_live_..."
    ```
  </Step>

  <Step title="Make your first call">
    <CodeGroup>
    ```bash curl
    curl https://mcp.thetahealth.cn/v1/chat/completions \
      -H "Authorization: Bearer $MIROBODY_API_KEY" \
      -H "Content-Type: application/json" \
      -d '{
            "model": "mirobody-flash",
            "messages": [{"role": "user", "content": "How is my fasting glucose trending?"}],
            "user": "alice"
          }'
    ```

    ```python Python (OpenAI SDK)
    from openai import OpenAI

    client = OpenAI(api_key="$MIROBODY_API_KEY", base_url="https://mcp.thetahealth.cn/v1")
    resp = client.chat.completions.create(
        model="mirobody-flash",
        messages=[{"role": "user", "content": "How is my fasting glucose trending?"}],
        user="alice",
    )
    print(resp.choices[0].message.content)
    ```

    ```javascript Node.js (OpenAI SDK)
    import OpenAI from "openai";

    const client = new OpenAI({
      apiKey: process.env.MIROBODY_API_KEY,
      baseURL: "https://mcp.thetahealth.cn/v1",
    });
    const resp = await client.chat.completions.create({
      model: "mirobody-flash",
      messages: [{ role: "user", content: "How is my fasting glucose trending?" }],
      user: "alice",
    });
    console.log(resp.choices[0].message.content);
    ```
    </CodeGroup>

    The `user` field isolates each end-user as a [Subject](/en/api-reference/overview#multi-tenancy-the-user-field) — pass each of your users' stable ids. See the full [Chat reference](/en/api-reference/chat).
  </Step>

  <Step title="Give the Subject some data">
    A fresh Subject has no data, so the agent will say so. Write a record (or upload a report with [`POST /v1/files`](/en/api-reference/files)) and ask again:

    ```bash
    curl https://mcp.thetahealth.cn/v1/data \
      -H "Authorization: Bearer $MIROBODY_API_KEY" \
      -H "Content-Type: application/json" \
      -d '{
            "user": "alice",
            "records": [{"indicator": "fasting_glucose", "value": 5.4, "unit": "mmol/L", "time": "2026-06-16T07:30:00Z"}]
          }'
    ```

    See [Data](/en/api-reference/data) and [Files](/en/api-reference/files).
  </Step>
</Steps>

## Prefer clicking over curl?

The console has a **Playground** — run chats, upload files, and inspect tool steps with no code.

<CardGroup cols={2}>
  <Card title="Playground" icon="flask" href="https://cdm.thetahealth.cn/developer">
    Try the agent in the browser against your account.
  </Card>
  <Card title="Usage" icon="chart-line" href="https://cdm.thetahealth.cn/developer/usage">
    Requests, tokens, and cost.
  </Card>
</CardGroup>

Testing against staging? Swap the base URL to `https://test-mcp.thetahealth.cn/v1` and the console to [test-cdm.thetahealth.cn](https://test-cdm.thetahealth.cn/).

## See it live

<CardGroup cols={2}>
  <Card title="mirobody.thetahealth.cn" icon="globe" href="https://mirobody.thetahealth.cn/">
    **Mirobody app** — the flagship consumer product, the same engine behind this API.
  </Card>
  <Card title="Developer console" icon="terminal" href="https://cdm.thetahealth.cn/developer">
    Create `mb_live_*` keys, view usage, and try the Playground.
  </Card>
</CardGroup>

## Next steps

<CardGroup cols={2}>
  <Card title="API Reference" icon="code" href="/en/api-reference/overview">
    Auth, multi-tenancy, retention, and every `/v1` endpoint.
  </Card>
  <Card title="SDK Examples" icon="boxes-stacked" href="/en/cloud/sdk-examples">
    curl / Python / Node, streaming and file upload.
  </Card>
  <Card title="China Region" icon="location-dot" href="/en/cloud/regions/china">
    Base URLs, storage residency, models, and limits.
  </Card>
  <Card title="Compliance" icon="shield" href="/en/cloud/compliance">
    PIPL / data residency; request a DPA.
  </Card>
</CardGroup>
