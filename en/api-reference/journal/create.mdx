---
title: "Create Journal"
description: "POST /api/v1/holywell/journal/create — create a journal entry"
---

## Endpoint

```http
POST /api/v1/holywell/journal/create
Authorization: Bearer <access_token>
Content-Type:  application/json
```

```json
{
  "id":         "<optional session_id, for idempotency>",
  "text_input": "Feeling energetic today",
  "files":      [ /* ChatFileObject array, see /api/chat */ ],
  "ts":         1779431900000
}
```

`text_input` and `files` — **at least one must be present**.

## Response

```json
{
  "code": 0, "msg": "ok",
  "data": {
    "input":        { "content":"Feeling energetic today", "file_list":[], "ts":1779431900000, "msg_ids":[] },
    "ts":           1779431900000,
    "session_id":   "today_504_2026_05_22_1779431370215",
    "read_status":  0,
    "write_status": 1,
    "ai_status":    0,
    "journal_id":   109,
    "summary":      null,
    "symptom":      null,
    "version":      1
  }
}
```

The server writes the journal row immediately; AI processing runs asynchronously. Poll [`/journal/list`](/en/api-reference/journal/list) until `ai_status == 1`.

**Recommended polling interval**: 2–5 seconds, up to 60s total.
