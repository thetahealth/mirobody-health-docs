---
title: "Production Deployment"
description: "Run the Mirobody C++ binary in production: secrets, backends, health checks, and compliance."
---

In production, Mirobody is the standalone `mirobody` binary (or the Docker image) driven by `config.yml` and environment variables, backed by your own database, cache, and object storage. This page covers the settings that matter beyond a local run; anything not verifiable in the engine is flagged.

## Production checklist

<AccordionGroup>
  <Accordion title="Security" icon="shield">
    - [ ] Replace every `REPLACE_THIS_VALUE_IN_PRODUCTION` placeholder in `config.yml`
    - [ ] Pre-encrypt secrets in `config.yml` with the `fernet` CLI and provide `CONFIG_ENCRYPTION_KEY` via the environment (it decrypts them on load)
    - [ ] Set a strong `JWT_KEY` (HS256) or `JWT_PRIVATE_KEY` (RS256)
    - [ ] Set `PG_ENCRYPTION_KEY` (encrypts sensitive data columns; keep it out of the file)
    - [ ] Remove `EMAIL_PREDEFINE_CODES` (demo codes are test-only)
    - [ ] Restrict CORS `Access-Control-Allow-Origin` to your domain
    - [ ] Terminate TLS at a reverse proxy / load balancer in front of the binary
  </Accordion>

  <Accordion title="Backends" icon="database">
    - [ ] Point `PG_*` at a managed PostgreSQL (desktop/server default backend)
    - [ ] Set `REDIS_HOST` if you run more than one instance (shared cache)
    - [ ] Configure an object-storage backend (S3 / OSS / Azure Blob) instead of local disk
  </Accordion>

  <Accordion title="Compliance (PHI)" icon="user-shield">
    - [ ] Keep the LLM on-device, or route hosted calls through a BAA-covered surface (Vertex AI / Azure OpenAI)
    - [ ] Self-host in your region and pin outbound dependencies to it (GDPR data residency)
    - [ ] Leave care-circle sharing off unless users opt in (it is off by default)
  </Accordion>

  <Accordion title="Operations" icon="chart-line">
    - [ ] Wire liveness/readiness probes to `/api/health`
    - [ ] Capture stderr with your process manager (`docker logs`, journald, …) and ship it to your aggregator
    - [ ] Automate PostgreSQL backups and test restores
  </Accordion>
</AccordionGroup>

## Secrets and keys

Generate strong values and provide the encryption key via the environment:

```bash
openssl rand -hex 32     # JWT_KEY (HS256), PG_ENCRYPTION_KEY, CONFIG_ENCRYPTION_KEY, LOCAL_STORAGE_SECRET
```

```bash
export CONFIG_ENCRYPTION_KEY="<generated>"
```

`CONFIG_ENCRYPTION_KEY` does two things: config values already stored as Fernet ciphertext (`gAAAA…`) are **transparently decrypted on load**, and secret-looking values are **masked** in the config summary dump. Nothing auto-encrypts plaintext on write — you pre-encrypt each secret yourself with the `fernet` CLI (`cli/fernet.cpp`: `fernet genkey` / `fernet encrypt`) and paste the `gAAAA…` token into `config.yml`. Done that way, you can commit an encrypted `config.yml` as long as the key stays in the environment. Keep bootstrap keys like `PG_ENCRYPTION_KEY` in the environment or a secrets manager.

For RS256 (so other services can verify your tokens against a published JWKS), set `JWT_PRIVATE_KEY` instead of `JWT_KEY`; `cli/jwt_keygen` mints a keypair.

## Example production config

```yaml config.yml
LOG_LEVEL: 'INFO'   # logs go to stderr — capture them via your process manager

HTTP_HOST: '0.0.0.0'
HTTP_PORT: 8080
HTTP_HEADERS:
  Access-Control-Allow-Origin: 'https://yourdomain.com'
  Access-Control-Allow-Methods: 'GET, POST, PUT, DELETE, OPTIONS'
  Access-Control-Allow-Headers: 'Authorization, Content-Type, X-Timezone'

# Database (managed PostgreSQL)
PG_HOST: 'your-db-host.rds.amazonaws.com'
PG_PORT: 5432
PG_USER: 'postgres'
PG_DBNAME: 'mirobody'
# PG_PASSWORD / PG_ENCRYPTION_KEY: provide via environment

# Cache (only needed for multi-instance)
REDIS_HOST: 'your-redis.cache.amazonaws.com'
REDIS_PORT: 6379
```

Provide the secrets separately:

```bash
export PG_PASSWORD="..."
export PG_ENCRYPTION_KEY="..."
export JWT_KEY="..."
export GOOGLE_API_KEY="..."      # or OPENAI_API_KEY
export CONFIG_ENCRYPTION_KEY="..."
```

## TLS and the reverse proxy

The binary serves plain HTTP + WebSocket on `HTTP_HOST:HTTP_PORT`. Terminate TLS in front of it (nginx, Caddy, a cloud load balancer) and forward to the binary's port. If you mount the app under a sub-path at the proxy, set `HTTP_URI_PREFIX` to match so generated URLs and routes line up. *(The engine does not terminate TLS itself — verify your proxy forwards WebSocket upgrade headers for the realtime `/api/chat` route.)*

## Health checks

```bash
curl https://api.yourdomain.com/api/health   # -> ok
```

`GET /api/health` is an unauthenticated liveness probe. Under Kubernetes, use it for both `httpGet` liveness and readiness probes (the Docker runtime image is libraries-only, so there is no in-container `curl` for a `HEALTHCHECK`).

## Running under Docker / Kubernetes

The shipped `Dockerfile` is written for this: it runs as an unprivileged user (uid 10001) and expects secrets/settings from env vars or a mounted `config.yml`. A few operational notes baked into the image:

- **Skip the in-process schema migration** in managed environments by setting `ENV=PROD` (or `TEST` / `GRAY`) — the server then assumes the schema is already applied.
- **Sentry crash reporting** writes to a database dir; the image points `SENTRY_DATABASE_PATH` at `/tmp/mirobody/sentry` so it stays writable under a read-only root filesystem.
- **Tanka login** pulls in `nodejs`; disable it (`TANKA_LOGIN_ENABLED=false`) if unused.

<Warning>
`ENV=PROD` selects the remote-config environment **and** skips migration. Only use it once your database schema is provisioned (from `res/sql/<backend>`). For a first-time self-hosted run against an empty database, let the default in-process migration create the schema.
</Warning>

## Object storage

Use a durable backend rather than local disk. Fill the S3, Alibaba OSS, or Azure Blob block in `config.yml` — see [Configuration → File storage](/en/configuration#file-storage). Pin the storage region to your compliance region.

## Compliance — HIPAA & GDPR

Because the core runs on-device or self-hosted, personal health data never has to leave your infrastructure. The single place PHI can leave is the LLM call:

- **HIPAA** — run the model **fully on-device** (Gemma 4; the native apps' "On-device" provider), or route hosted models through a BAA-eligible surface: **Google Gemini via Vertex AI** (`GOOGLE_GENAI_USE_VERTEXAI=1` + `GCP_PROJECT` / `VERTEX_LOCATION`) or **OpenAI via Azure OpenAI** (`AZURE_OPENAI_ENDPOINT` + deployment + key). The public AI Studio / OpenAI-direct endpoints are not BAA-covered.
- **GDPR** — self-host in your region and pin every outbound dependency to it: `VERTEX_LOCATION` / `VERTEX_BASE_URL` for Gemini, the Azure resource region for GPT, the object-storage region per backend, and `AZURE_BLOB_ENDPOINT_SUFFIX` for sovereign clouds.

Care-circle sharing is opt-in and off by default, and the AI assistant is **read-only** over another member's data. See [Compliance](/en/api-reference/compliance) for the managed-cloud posture.

## Scaling

- **Horizontal** — run multiple instances behind a load balancer; set `REDIS_HOST` so they share cache state. All instances talk to the one configured PostgreSQL (`PG_HOST` + a connection pool) — the engine has no read-replica routing, so scale the database itself if it becomes the bottleneck. *(Sticky sessions are not required for the SSE/WebSocket chat routes as long as each connection stays on one instance for its lifetime — verify against your load balancer's connection handling.)*
- **Vertical** — the C++ binary is lightweight; size CPU/memory to your LLM concurrency and document-processing load.

## Monitoring

The binary logs to **stderr** (`src/platform/log.cpp`); there is no log-file option — capture it with your process manager or `docker logs`. Metrics worth watching:

- Chat turn latency and error rate
- Provider sync success rate
- Database connection health
- Cache hit rate (Redis, if enabled)
- CPU / memory per instance

*(Mirobody does not ship a Prometheus/metrics endpoint that we can confirm here — instrument at the proxy / platform layer, or verify against the current build.)*

## Backups

```bash
# Daily PostgreSQL dump (cron)
0 2 * * * pg_dump -h <host> -U postgres mirobody | gzip > /backups/mirobody-$(date +\%Y\%m\%d).sql.gz
```

Also back up your `config.yml` (encrypted) and the object-storage bucket. Test restores regularly.

## Next Steps

<CardGroup cols={2}>
  <Card title="Docker Deployment" icon="docker" href="/en/deployment/docker">
    Build and run the container image
  </Card>
  <Card title="Configuration" icon="gear" href="/en/configuration">
    Every production key in one place
  </Card>
  <Card title="Compliance" icon="user-shield" href="/en/api-reference/compliance">
    HIPAA / GDPR posture
  </Card>
  <Card title="API Reference" icon="code" href="/en/api-reference/overview">
    Endpoints your clients will call
  </Card>
</CardGroup>
