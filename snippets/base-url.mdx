The live integration environment today is the global **test** cluster:

```text
https://test-mirobody-api.thetahealth.ai/v1
```

Its data is non-production and may be reset at any time — never put real end-user data there. See [Regions](/en/api-reference/regions/overview) for the full host matrix and live status.
