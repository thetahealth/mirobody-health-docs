All `/v1` endpoints share one base URL — pick your contracted cluster:

```text
https://api.mirobody.ai/v1
```

China-region deployments use `https://api.mirobody.cn/v1`. See [Regions](/en/api-reference/regions/overview) for the full host matrix.
