All `/v1` endpoints share one base URL — pick your contracted cluster:

```text
https://api.mirobody.ai/v1
```

The planned China production base URL is `https://api.mirobody.cn/v1`; it is not yet a public production endpoint. See [Regions](/en/api-reference/regions/overview) for current availability.
