健康数据有三种形态 —— 每种形态各有一扇门进来：

| 数据形态 | 典型来源 | 录入方式 | 端点 |
| --- | --- | --- | --- |
| **结构化读数** | 设备/穿戴（**由你自己接入**，日聚合后写入）、手动记录 | 结构化记录 | [`POST /v1/data`](/zh/api-reference/data) —— 高频样本先自聚合；episode 型（睡眠、运动）用 `time` + `end_time` 携带时段。见[设备数据 cookbook](/zh/api-reference/device-data)。 |
| **文件/照片** | 化验单、体检报告 PDF、手机拍照 | 文件上传 | [`POST /v1/files`](/zh/api-reference/files) —— 自动 OCR 与解析；想同步拿回解析结果用 [`POST /v1/extract`](/zh/api-reference/extract) |
| **叙事记录** | "我头晕头疼"、对话口述 | 叙事文本 | [`POST /v1/extract`](/zh/api-reference/extract) —— 可量化读数会被抽取；纯叙事返回 `data: []` 加一个 `note`（journal 端点在规划中） |

Mirobody 负责托管、标准化并把数据供给 AI —— 采集数据（设备 OAuth、App 侧获取）在你那一侧。
